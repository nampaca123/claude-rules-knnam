# CLAUDE.md

이 프로젝트에서 Claude가 따르는 최상위 규칙이다. 세부 규칙은 `rules/` 하위 모듈에 두고, 이 파일은 그것들을 연결하는 브로커 역할만 한다.

## 작업 전 원칙

Andrej Karpathy의 4가지 기본 자세를 기본값으로 한다.

1. **Think Before Coding.** 가정이 있으면 명시한다. 해석이 여러 갈래로 갈리면 그 갈래를 사용자에게 보여주고 임의로 고르지 않는다. 더 단순한 길이 보이면 말한다. 모호하면 멈추고 묻는다.
2. **Simplicity First.** 요청된 것만 만든다. 사변적 추상화, 요청되지 않은 유연성, 불가능한 시나리오를 위한 예외 처리는 넣지 않는다. 200줄로 쓴 게 50줄로 줄어들 수 있으면 다시 쓴다.
3. **Surgical Changes.** 건드려야 하는 부분만 국소적으로 건드린다. 인접한 코드의 스타일·포맷·주석은 손대지 않는다. 변경으로 인해 죽은 코드가 생기면 그것만 제거한다. 변경된 모든 라인은 사용자의 요청에 직접 닿아 있어야 한다.
4. **Goal-Driven Execution.** "기능을 추가해줘"는 "배포 환경에서 테스트가 통과하는 상태"와 같다. "버그 고쳐줘"는 "재현 테스트를 만들고 그것을 통과시킨다"로 환원한다. 다단계 작업은 짧은 계획을 먼저 제시한다.

## 규칙 모듈

작업 성격에 따라 아래 모듈을 함께 읽는다. 본 파일과 모순되면 본 파일이 우선한다.

- @rules/respond/_index.md — 사용자와 어떻게 소통하는가 (한국어, 계획 우선, 단계별 보고)
- @rules/quality/_index.md — 코드 컨벤션, 파일 구조, 계층화, 문서 동기화
- @rules/test/_index.md — 테스트 방법론, 2중 게이트, `docs/test/` 아카이브
- @rules/framework/_index.md — 프레임워크별 필수 원칙 (Spring Boot, FastAPI, AWS, Next.js, Nest.js)

## 절대 규칙

다음은 어떤 상황에서도 양보하지 않는다.

1. 사용자와의 대화는 한국어로 한다. 코드·식별자·파일명·명령어·에러 메시지는 영어 원문을 그대로 둔다.
2. `schema.sql`은 DB 스키마의 single source of truth이다. ORM 정의를 바꾸면 같은 PR에서 `schema.sql`도 바꾼다.
3. `README.md`는 항상 코드의 현재 상태를 반영한다. 기능·환경 변수·실행 명령·의존성이 바뀌면 같은 PR에서 갱신한다.
4. 비밀(API 키, 토큰, DB 비밀번호, URL)은 절대 코드나 저장소에 하드코딩하지 않으며, fallback도 담지 않는다. 이들은 모두 .env에 담는다.
5. 배포 후 배포된 서버에 대해 curl 스모크 테스트가 통과하기 전까지 "완료"라고 보고하지 않는다.
6. 코드 작성 전에 계획을 먼저 제시하고 사용자 확인을 받는다. 다단계 작업일수록 더 엄격하게 한다.
